/**
 * Run Shop Page Filtering Tests
 * 
 * This script runs the shop page filtering tests to verify that the product
 * filtering functionality works correctly.
 */

import runTests from './shopPageFiltering.test.js';

console.log('Running Shop Page Filtering Tests...');
runTests();