# CropsayAI System Diagrams

This document contains diagrams of the CropsayAI system with detailed explanations.

## Object Diagram

The Object Diagram illustrates the key data entities in the CropsayAI system and their relationships.

**Detailed Explanation:**
- **User**: Represents an end user with attributes like id, name, and email. Each user can have multiple chat sessions.
- **ChatSession**: Represents a conversation between a user and the AI, containing a unique id, reference to the user, and a timestamp.
- **Message**: Represents individual messages within a chat session, with content, sender identifier, timestamp, and session reference.
- **Product**: Represents agricultural products in the catalog with attributes like id, name, description, category, and price.
- **Recommendation**: Represents product recommendations generated by the AI, including product reference, relevance score, and reasoning.
- **GeminiResponse**: Represents the response generated by the Gemini AI model, containing response text and metadata.

```plantuml
@startuml Object Diagram
!theme plain
skinparam monochrome true
skinparam shadowing false
skinparam defaultFontName Arial
skinparam defaultFontSize 12
skinparam backgroundColor white
skinparam ObjectBackgroundColor white
skinparam ObjectBorderColor black
skinparam ArrowColor black
skinparam linetype ortho
skinparam nodesep 80
skinparam ranksep 100

object "User" as user {
  id: String
  name: String
  email: String
}

object "ChatSession" as chatSession {
  id: String
  userId: String
  timestamp: DateTime
}

object "Message" as message {
  id: String
  text: String
  sender: String
  timestamp: DateTime
  sessionId: String
}

object "Product" as product {
  id: String
  name: String
  description: String
  category: String
  price: Number
}

object "Recommendation" as recommendation {
  id: String
  productId: String
  relevanceScore: Number
  reasoning: String
}

object "GeminiResponse" as geminiResponse {
  text: String
  messageId: String
  timestamp: DateTime
}

user -- chatSession : has >
chatSession -- message : contains >
message -- recommendation : generates >
message -- geminiResponse : has >
recommendation -- product : references >

@enduml
```

## Class Diagram

The Class Diagram shows the software architecture of CropsayAI, organized into packages for Frontend, Services, and Data.

**Detailed Explanation:**
- **Frontend Package**: Contains UI-related classes that handle user interactions and display.
- **Services Package**: Contains the core business logic and service classes.
- **Data Package**: Contains classes that manage data access and storage.

```plantuml
@startuml Class Diagram
!theme plain
skinparam monochrome true
skinparam shadowing false
skinparam defaultFontName Arial
skinparam defaultFontSize 12
skinparam backgroundColor white
skinparam ClassBackgroundColor white
skinparam ClassBorderColor black
skinparam PackageBackgroundColor white
skinparam PackageBorderColor black
skinparam ArrowColor black
skinparam linetype ortho
skinparam nodesep 60
skinparam ranksep 80

package "Frontend" {
  class ChatPage {
    - messages: Message[]
    - recommendations: Product[]
    + handleSendMessage(message: string): void
    + handleProductClick(product: Product): void
  }
  
  class ExpertPanel {
    - recommendations: Product[]
    + displayRecommendations(): void
  }
}

package "Services" {
  class GeminiService {
    - API_KEY: string
    - MODEL_NAME: string
    + generateResponse(message: string, history: string): Promise<string>
    - formatPrompt(message: string, history: string): string
  }
  
  class RecommendationService {
    + getRecommendations(message: string, history: string): Promise<Product[]>
    - rankRecommendations(products: Product[]): Product[]
  }
  
  class NLPRecommendationService {
    - model: TransformerModel
    - productEmbeddings: number[][]
    + getRecommendations(query: string): Product[]
    - analyzeIntent(query: string): IntentAnalysis
  }
}

package "Data" {
  class AgriculturalKnowledgeGraph {
    - crops: Map<string, CropInfo>
    - problems: Map<string, ProblemInfo>
    + getRelatedProducts(entity: string): Product[]
  }
  
  class ProductDatabase {
    - products: Product[]
    + getProducts(): Product[]
    + getProductById(id: string): Product
  }
}

ChatPage --> GeminiService
ChatPage --> RecommendationService
ExpertPanel --> RecommendationService
RecommendationService --> NLPRecommendationService
RecommendationService --> ProductDatabase
NLPRecommendationService --> AgriculturalKnowledgeGraph

@enduml
```

## State Diagram

The State Diagram depicts the different states of the CropsayAI chat system and the transitions between them.

**Detailed Explanation:**
This diagram shows how the system changes states during user interaction, from idle to processing input, displaying results, and handling errors.

```plantuml
@startuml State Diagram
!theme plain
skinparam monochrome true
skinparam shadowing false
skinparam defaultFontName Arial
skinparam defaultFontSize 12
skinparam backgroundColor white
skinparam StateBackgroundColor white
skinparam StateBorderColor black
skinparam ArrowColor black
skinparam linetype ortho
skinparam nodesep 60
skinparam ranksep 80

[*] --> Idle

state Idle {
  [*] --> WaitingForUserInput
}

state "Processing User Input" as Processing {
  [*] --> ValidatingInput
  ValidatingInput --> GeneratingResponse : Valid Input
  ValidatingInput --> WaitingForUserInput : Invalid Input
  GeneratingResponse --> GeneratingRecommendations
  GeneratingRecommendations --> DisplayingResults
}

state "Displaying Results" as Displaying {
  [*] --> ShowingAIResponse
  ShowingAIResponse --> ShowingRecommendations
  ShowingRecommendations --> WaitingForUserInput
}

state "Error Handling" as Error {
  [*] --> DetectingError
  DetectingError --> AttemptingRetry
  AttemptingRetry --> GeneratingResponse : Retry Success
  AttemptingRetry --> ShowingErrorMessage : Retry Failed
  ShowingErrorMessage --> WaitingForUserInput
}

WaitingForUserInput --> ValidatingInput : User Sends Message
DisplayingResults --> WaitingForUserInput : Display Complete
GeneratingResponse --> DetectingError : Error Occurs
GeneratingRecommendations --> DetectingError : Error Occurs

@enduml
```

## Sequence Diagram

The Sequence Diagram illustrates the interaction between different components of the CropsayAI system over time.

**Detailed Explanation:**
This diagram shows the precise sequence of operations that occur when a user sends a message, including message storage, AI response generation, and recommendation generation.

```plantuml
@startuml Sequence Diagram
!theme plain
skinparam monochrome true
skinparam shadowing false
skinparam defaultFontName Arial
skinparam defaultFontSize 12
skinparam backgroundColor white
skinparam SequenceLifeLineBackgroundColor white
skinparam SequenceLifeLineBorderColor black
skinparam SequenceParticipantBackgroundColor white
skinparam SequenceParticipantBorderColor black
skinparam SequenceActorBackgroundColor white
skinparam SequenceActorBorderColor black
skinparam ArrowColor black
skinparam SequenceArrowThickness 1
skinparam ParticipantPadding 30

actor User
participant "React Frontend" as Frontend
participant "ChatService" as ChatService
participant "GeminiService" as GeminiService
participant "RecommendationService" as RecommendationService
participant "NLPService" as NLPService
participant "Supabase" as Database

User -> Frontend: Send message
activate Frontend

Frontend -> ChatService: saveMessage(userMessage)
activate ChatService
ChatService -> Database: Insert message
Database --> ChatService: Confirmation
ChatService --> Frontend: Message saved
deactivate ChatService

Frontend -> GeminiService: generateResponse(message, history)
activate GeminiService
GeminiService --> Frontend: AI response
deactivate GeminiService

Frontend -> ChatService: saveMessage(aiMessage)
activate ChatService
ChatService -> Database: Insert message
Database --> ChatService: Confirmation
ChatService --> Frontend: Message saved
deactivate ChatService

Frontend -> RecommendationService: getRecommendations(message, history)
activate RecommendationService
RecommendationService -> NLPService: Get recommendations
activate NLPService
NLPService --> RecommendationService: Product recommendations
deactivate NLPService
RecommendationService --> Frontend: Recommendations
deactivate RecommendationService

Frontend -> Frontend: Update UI with response and recommendations
Frontend --> User: Display AI response and recommendations

deactivate Frontend

@enduml
```

## Activity Diagram

The Activity Diagram shows the flow of activities in the CropsayAI system, focusing on the processing of user queries and generation of responses and recommendations.

**Detailed Explanation:**
This diagram illustrates the step-by-step activities that occur when processing a user query, including parallel processing paths for AI response generation and recommendation generation.

```plantuml
@startuml Activity Diagram
!theme plain
skinparam monochrome true
skinparam shadowing false
skinparam defaultFontName Arial
skinparam defaultFontSize 12
skinparam backgroundColor white
skinparam ActivityBackgroundColor white
skinparam ActivityBorderColor black
skinparam ArrowColor black
skinparam linetype ortho
skinparam ActivityAttributeWidth 60
skinparam ActivityAttributeHeight 40

start

:Receive user query;

:Validate query for agricultural relevance;

if (Is query agriculture-related?) then (yes)
  fork
    :Format query with agricultural context;
    :Send to Gemini AI;
    :Process AI response;
    :Format response for display;
  fork again
    :Extract agricultural entities and intent;
    :Retrieve relevant products;
    :Calculate similarity scores;
    :Rank products by relevance;
    :Generate reasoning for recommendations;
  end fork
  
  :Combine AI response and recommendations;
  :Display results to user;
  :Log interaction;
  
  repeat
    :Wait for user interaction;
    
    if (User clicks on product?) then (yes)
      :Display product details;
      if (User adds to cart?) then (yes)
        :Update shopping cart;
      endif
    endif
    
  repeat while (User continues conversation?) is (yes)
  
  if (User proceeds to checkout?) then (yes)
    :Process payment;
    :Generate order confirmation;
    :Send confirmation to user;
  endif
  
else (no)
  :Prompt user for agriculture-related query;
  :Return to waiting state;
endif

stop

@enduml
```

## Refinement of Classes and Objects

The Refinement of Classes and Objects Diagram provides a more detailed view of the CropsayAI class structure, showing additional attributes, methods, and relationships between classes.

**Detailed Explanation:**
This diagram expands on the basic class diagram by providing more implementation details for frontend components, core services, specialized services, and data management.

```plantuml
@startuml Refined Class Diagram
!theme plain
skinparam monochrome true
skinparam shadowing false
skinparam defaultFontName Arial
skinparam defaultFontSize 12
skinparam backgroundColor white
skinparam ClassBackgroundColor white
skinparam ClassBorderColor black
skinparam PackageBackgroundColor white
skinparam PackageBorderColor black
skinparam ArrowColor black
skinparam linetype ortho
skinparam nodesep 70
skinparam ranksep 90

package "Frontend Components" {
  class ChatPage {
    - messages: Message[]
    - recommendations: Product[]
    - isLoading: boolean
    + handleSendMessage(message: string): void
    + handleProductClick(product: Product): void
  }
  
  class ExpertPanel {
    - recommendations: Product[]
    - isVisible: boolean
    + displayRecommendations(): void
    + toggleVisibility(): void
  }
  
  class Message {
    - id: string
    - text: string
    - sender: "user" | "ai"
    - timestamp: Date
    + formatTimestamp(): string
  }
  
  class Product {
    - id: string
    - name: string
    - description: string
    - category: string
    - price: number
    - inStock: boolean
    + getFormattedPrice(): string
  }
}

package "Core Services" {
  class GeminiService {
    - API_KEY: string
    - MODEL_NAME: string
    + generateResponse(message: string, history: string): Promise<string>
    - formatPrompt(message: string, history: string): string
  }
  
  class RecommendationService {
    - geminiRecommendationService: GeminiRecommendationService
    - nlpRecommendationService: NLPBridgeService
    + getRecommendations(message: string, history: string): Promise<Product[]>
    - mergeRecommendations(recommendations: Product[][]): Product[]
  }
  
  class ChatService {
    - supabaseClient: SupabaseClient
    - currentUser: User | null
    + saveMessage(message: Message): Promise<void>
    + getMessages(sessionId: string): Promise<Message[]>
    + createSession(): Promise<string>
  }
}

package "Specialized Services" {
  class GeminiRecommendationService {
    - API_KEY: string
    + getRecommendations(message: string, products: Product[], limit: number): Promise<Product[]>
  }
  
  class NLPBridgeService {
    - serviceUrl: string
    - isAvailable: boolean
    + getRecommendations(query: string, limit: number): Promise<Product[]>
  }
}

package "Data Management" {
  class AgriculturalKnowledgeGraph {
    - crops: Map<string, CropInfo>
    - problems: Map<string, ProblemInfo>
    + getRelatedProducts(entity: string): Product[]
    + getEntityInfo(entity: string, type: string): any
  }
  
  class ProductDatabase {
    - products: Product[]
    - categories: string[]
    + getProducts(): Product[]
    + getProductById(id: string): Product
    + getProductsByCategory(category: string): Product[]
  }
}

ChatPage --> Message
ChatPage --> ExpertPanel
ExpertPanel --> Product
ChatPage --> GeminiService
ChatPage --> RecommendationService
ChatPage --> ChatService
RecommendationService --> GeminiRecommendationService
RecommendationService --> NLPBridgeService
GeminiRecommendationService --> ProductDatabase
NLPBridgeService --> AgriculturalKnowledgeGraph
ChatService --> ProductDatabase

@enduml
```

## Component Diagram

The Component Diagram shows the high-level components of the CropsayAI system and their dependencies.

**Detailed Explanation:**
This diagram provides a comprehensive view of the system's architecture at the component level, showing how frontend components, backend services, external services, and data stores interact with each other.

```plantuml
@startuml Component Diagram
!theme plain
skinparam monochrome true
skinparam shadowing false
skinparam defaultFontName Arial
skinparam defaultFontSize 12
skinparam backgroundColor white
skinparam ComponentBackgroundColor white
skinparam ComponentBorderColor black
skinparam PackageBackgroundColor white
skinparam PackageBorderColor black
skinparam ArrowColor black
skinparam linetype ortho
skinparam nodesep 70
skinparam ranksep 90

package "Frontend" {
  [React UI Components] as UI
  [State Management] as State
  [API Client] as APIClient
}

package "Backend Services" {
  [Gemini Service] as GeminiService
  [Gemini Proxy] as GeminiProxy
  [Recommendation Service] as RecommendationService
  [Chat Service] as ChatService
  [NLP Service] as NLPService
}

package "External Services" {
  [Google Gemini API] as GeminiAPI
  [Supabase] as Supabase
}

package "Data Stores" {
  [Product Database] as ProductDB
  [Agricultural Knowledge Graph] as KnowledgeGraph
  [User Data] as UserData
  [Chat History] as ChatHistory
}

UI --> State
State --> APIClient
APIClient --> GeminiService
APIClient --> RecommendationService
APIClient --> ChatService

GeminiService --> GeminiProxy
GeminiProxy --> GeminiAPI
RecommendationService --> GeminiService
RecommendationService --> NLPService
RecommendationService --> ProductDB
RecommendationService --> KnowledgeGraph

NLPService --> ProductDB
NLPService --> KnowledgeGraph

ChatService --> Supabase
ChatService --> UserData
ChatService --> ChatHistory

@enduml
```

## System Architecture Overview

The System Architecture Overview Diagram provides a high-level view of the CropsayAI system architecture. It shows the layered structure of the system, from frontend to data layer, and the relationships between different layers.

**Detailed Explanation:**
This diagram shows the layered architecture of the CropsayAI system, including the Frontend Layer, Service Layer, Integration Layer, External Services, and Data Layer.

```plantuml
@startuml System Architecture Overview
!theme plain
skinparam monochrome true
skinparam shadowing false
skinparam defaultFontName Arial
skinparam defaultFontSize 12
skinparam backgroundColor white
skinparam PackageBackgroundColor white
skinparam PackageBorderColor black
skinparam ArrowColor black
skinparam linetype ortho
skinparam nodesep 70
skinparam ranksep 90

top to bottom direction

package "Frontend Layer" {
  [React UI Components]
  [State Management]
  [API Client]
}

package "Service Layer" {
  [Gemini Service]
  [Recommendation Service]
  [Chat Service]
  [Authentication Service]
}

package "Integration Layer" {
  [Gemini Proxy]
  [NLP Bridge]
  [Supabase Client]
}

package "External Services" {
  [Google Gemini API]
  [Python NLP Service]
  [Supabase]
}

package "Data Layer" {
  [Product Database]
  [Agricultural Knowledge Graph]
  [User Data]
  [Chat History]
}

[React UI Components] --> [State Management]
[State Management] --> [API Client]

[API Client] --> [Gemini Service]
[API Client] --> [Recommendation Service]
[API Client] --> [Chat Service]
[API Client] --> [Authentication Service]

[Gemini Service] --> [Gemini Proxy]
[Recommendation Service] --> [NLP Bridge]
[Chat Service] --> [Supabase Client]
[Authentication Service] --> [Supabase Client]

[Gemini Proxy] --> [Google Gemini API]
[NLP Bridge] --> [Python NLP Service]
[Supabase Client] --> [Supabase]

[Python NLP Service] --> [Product Database]
[Python NLP Service] --> [Agricultural Knowledge Graph]
[Supabase] --> [User Data]
[Supabase] --> [Chat History]

@enduml
